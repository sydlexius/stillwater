coverage:
  patch:
    default:
      # Threshold is checked against patch lines only.
      # Generated template files (*_templ.go) are excluded below.
      threshold: 70%

ignore:
  # Templ codegen output -- byte-for-byte compiler output, not hand-written.
  - "web/templates/**_templ.go"
