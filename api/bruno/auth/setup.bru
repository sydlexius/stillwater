meta {
  name: Setup Admin Account
  type: http
  seq: 1
}

post {
  url: {{apiBase}}/auth/setup
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "changeme123"
  }
}

tests {
  test("should return 201", function() {
    expect(res.status).to.equal(201);
  });
}
