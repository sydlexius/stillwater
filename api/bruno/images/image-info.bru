meta {
  name: Image Info
  type: http
  seq: 3
}

get {
  url: {{apiBase}}/artists/{{artistId}}/images/{{imageType}}/info
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
