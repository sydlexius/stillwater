meta {
  name: Update Webhook
  type: http
  seq: 4
}

put {
  url: {{apiBase}}/webhooks/{{webhookId}}
  body: json
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

body:json {
  {
    "name": "Updated Webhook"
  }
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
