meta {
  name: Create Backup
  type: http
  seq: 1
}

post {
  url: {{apiBase}}/settings/backup
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });

  test("should return backup info", function() {
    expect(res.body.filename).to.be.a("string");
  });
}
