meta {
  name: List Notifications
  type: http
  seq: 1
}

get {
  url: {{apiBase}}/notifications?page=1&page_size=50
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
