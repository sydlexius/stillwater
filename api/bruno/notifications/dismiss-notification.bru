meta {
  name: Dismiss Notification
  type: http
  seq: 3
}

post {
  url: {{apiBase}}/notifications/{{notificationId}}/dismiss
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
