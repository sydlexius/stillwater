meta {
  name: Health History
  type: http
  seq: 2
}

get {
  url: {{apiBase}}/reports/health/history
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });

  test("should return history array", function() {
    expect(res.body.history).to.be.an("array");
  });
}
