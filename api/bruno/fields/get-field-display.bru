meta {
  name: Get Field Display
  type: http
  seq: 1
}

get {
  url: {{apiBase}}/artists/{{artistId}}/fields/{{fieldName}}/display
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
