meta {
  name: Bulk Fetch Metadata
  type: http
  seq: 1
}

post {
  url: {{apiBase}}/bulk/fetch-metadata
  body: json
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

body:json {
  {
    "mode": "prompt_no_match"
  }
}

tests {
  test("should return 202", function() {
    expect(res.status).to.equal(202);
  });
}
