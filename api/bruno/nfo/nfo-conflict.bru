meta {
  name: NFO Conflict Check
  type: http
  seq: 2
}

get {
  url: {{apiBase}}/artists/{{artistId}}/nfo/conflict
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
