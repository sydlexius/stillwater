meta {
  name: List Providers
  type: http
  seq: 1
}

get {
  url: {{apiBase}}/providers
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });

  test("should return providers array", function() {
    expect(res.body.providers).to.be.an("array");
  });
}
