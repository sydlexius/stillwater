meta {
  name: Get Priorities
  type: http
  seq: 5
}

get {
  url: {{apiBase}}/providers/priorities
  body: none
  auth: none
}

headers {
  Cookie: session={{sessionToken}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });

  test("should return priorities", function() {
    expect(res.body.priorities).to.be.an("array");
  });
}
